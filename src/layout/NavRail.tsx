import { FolderKanban, History, Maximize2, MessageCircle, Minus, Palette, Settings, X } from 'lucide-react';
import { IconButton } from '../components/IconButton';
import { useAppStore } from '../stores/appStore';
import { t } from '../lib/i18n';
import { getCurrentWindow } from '@tauri-apps/api/window';
import type { MouseEvent as ReactMouseEvent } from 'react';
export function NavRail() { const { view, setView, settings } = useAppStore(); const window = getCurrentWindow(); const items = [{ key: 'chat' as const, icon: MessageCircle, label: t(settings.language, 'chat') }, { key: 'projects' as const, icon: FolderKanban, label: t(settings.language, 'projects') }, { key: 'history' as const, icon: History, label: t(settings.language, 'history') }]; const control = (action: () => Promise<void>) => (event: ReactMouseEvent<HTMLButtonElement>) => { event.preventDefault(); event.stopPropagation(); void action().catch(console.error); }; return <nav className="nav-rail"><div className="window-controls"><button type="button" title="关闭窗口" aria-label="关闭窗口" data-tooltip="关闭窗口" className="window-close" onClick={control(() => window.close())}><X/></button><button type="button" title="最小化窗口" aria-label="最小化窗口" data-tooltip="最小化窗口" className="window-minimize" onClick={control(() => window.minimize())}><Minus/></button><button type="button" title="最大化或退出最大化" aria-label="最大化或退出最大化" data-tooltip="最大化或退出最大化" className="window-maximize" onClick={control(() => window.toggleMaximize())}><Maximize2/></button></div><div className="account-avatar" title="wedex" data-tooltip="wedex">W</div><div className="nav-divider"/>{items.map(({ key, icon: Icon, label }) => <IconButton key={key} title={label} className={view === key ? 'active' : ''} onClick={() => setView(key)}><Icon size={23} strokeWidth={1.8}/></IconButton>)}<div className="nav-spacer" /><IconButton title={t(settings.language, 'customize')} className={view === 'customize' ? 'active' : ''} onClick={() => setView('customize')}><Palette size={21} strokeWidth={1.8}/></IconButton><IconButton title={t(settings.language, 'settings')} className={view === 'settings' ? 'active' : ''} onClick={() => setView('settings')}><Settings size={21} strokeWidth={1.8}/></IconButton><div className="user-dot" title={t(settings.language, 'account')} data-tooltip={t(settings.language, 'account')}>W</div></nav>; }
