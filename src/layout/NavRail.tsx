import { FolderKanban, History, MessageCircle, Palette, Settings } from 'lucide-react';
import { IconButton } from '../components/IconButton';
import { useAppStore } from '../stores/appStore';
import { t } from '../lib/i18n';
export function NavRail() { const { view, setView, settings } = useAppStore(); const items = [{ key: 'chat' as const, icon: MessageCircle, label: t(settings.language, 'chat') }, { key: 'projects' as const, icon: FolderKanban, label: t(settings.language, 'projects') }, { key: 'history' as const, icon: History, label: t(settings.language, 'history') }]; return <nav className="nav-rail"><div className="account-avatar" title="wedex" data-tooltip="wedex">W</div><div className="nav-divider"/>{items.map(({ key, icon: Icon, label }) => <IconButton key={key} title={label} className={view === key ? 'active' : ''} onClick={() => setView(key)}><Icon size={23} strokeWidth={1.8}/></IconButton>)}<div className="nav-spacer" /><IconButton title={t(settings.language, 'customize')} className={view === 'customize' ? 'active' : ''} onClick={() => setView('customize')}><Palette size={21} strokeWidth={1.8}/></IconButton><IconButton title={t(settings.language, 'settings')} className={view === 'settings' ? 'active' : ''} onClick={() => setView('settings')}><Settings size={21} strokeWidth={1.8}/></IconButton><div className="user-dot" title={t(settings.language, 'account')} data-tooltip={t(settings.language, 'account')}>W</div></nav>; }
