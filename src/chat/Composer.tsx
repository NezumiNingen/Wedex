import { Paperclip, Send, Square, Trash2 } from 'lucide-react';
import { useState } from 'react';
import { useAppStore } from '../stores/appStore';
export function Composer() { const [text, setText] = useState(''); const { send, running, cancel, selectedProjectId, settings } = useAppStore(); const submit = () => { void send(text); setText(''); }; return <footer className="composer"><div className="composer-tools"><button title="添加上下文"><Paperclip size={17}/></button><span>{settings.permission}</span><span className="tokens">{text.length} 字符</span></div><textarea disabled={!selectedProjectId} value={text} onChange={(e) => setText(e.target.value)} onKeyDown={(e) => { if ((e.metaKey || e.ctrlKey) && e.key === 'Enter') { e.preventDefault(); submit(); } }} placeholder={selectedProjectId ? '描述要交给 Codex 的任务…（⌘/Ctrl + Enter 发送）' : '请先添加并选择一个项目'} rows={3}/><div className="composer-footer"><button className="clear" onClick={() => setText('')}><Trash2 size={15}/>清空</button>{running ? <button className="stop" onClick={cancel}><Square size={15}/>停止</button> : <button className="send" disabled={!text.trim() || !selectedProjectId} onClick={submit}><Send size={16}/>发送</button>}</div></footer>; }
