# Contributing

Please open an issue before large changes. Use `pnpm lint`, `pnpm test`, and `pnpm build` before opening a pull request. Do not include project files, access tokens, local databases, or Codex credentials in commits.
